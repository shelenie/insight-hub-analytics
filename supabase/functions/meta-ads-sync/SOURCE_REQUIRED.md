# Source required for `meta-ads-sync`

This Edge Function exists in Supabase at:

https://iaxqonjwgrqpgvbeydok.supabase.co/functions/v1/meta-ads-sync

Real deployed source must be copied manually from Supabase before this repository can become the source of truth for this function.

- Do not rename `index.ts.todo` to `index.ts` until the real deployed source is pasted.
- Do not deploy incomplete source.
- Preserve this function's current Verify JWT setting from Supabase when migrating source.
- Never paste secrets into source code; use environment variables or Supabase secrets instead.
- Never log raw `access_token` or `refresh_token` values.
- After real source is added, remove this `SOURCE_REQUIRED.md` if no longer needed.
